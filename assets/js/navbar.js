/*

$(window).scroll(function () {
    var wScroll = $(this).scrollTop();

    if (wScroll >= servicePos) {
        $navbarItem.animate({
            top : '5px'
        });
    } else {
        $navbarItem.animate({
            borderBottomWidth : '0'
        });
    }
});

*/